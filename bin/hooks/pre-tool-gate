#!/usr/bin/env bash
# Compat shim — the gate is compiled into the bbs binary as
# `bbs hooks pre-tool-gate` (internal/cmd/hooks.go). This script stays only so
# manifests that invoke it by path keep working; it needs bbs on PATH, the
# same contract hooks/hooks.json already relies on.
exec bbs hooks pre-tool-gate "$@"
